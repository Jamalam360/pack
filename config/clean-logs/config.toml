# Print out all phrases and regex to be filtered out on startup
# default: true
printOnStart = false
# Print a single line on start saying that Clean Logs will filter from this point forward
# default: false
printInitLineOnStart = true
phrases = [
  "ModuleLoader",
  "Saving block-graph",
  "Ambiguity between arguments [teleport, location]",
  "Missing sound for event:",
  "'language' is an unsupported metadata key!",
  "campanion:textures/block/rope_bridge_planks.png",
  "No Valid Discord Client was found for this Instance",
  "Encountered duplicate API provider registration",
  "MixinConfig",
  "Force-disabling mixin",
  "STDOUT",
  "Fixing MC-",
  "No data fixer registered",
  "@Redirect conflict",
  "cannot add it to event charmonium:situational.underground_water",
  "Using missing texture",
  ".minecraft\\config\\byg",
  ".minecraft/config/byg",
  "Unknown item 'minecraft:sunmetal_blend'",
] # If a log message has one of these phrases, it will be filtered out from logging
# If a log message matches one of these regex patterns, it will be filtered out from logging
regex = []
